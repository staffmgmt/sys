// frontend/src/utils/apiConfig.ts (Phase 3 - Simplified)

// Base URL configuration - can be adjusted based on environment variables
// Example: process.env.REACT_APP_API_BASE_URL || 'http://localhost:8000';
export const API_BASE_URL = 'http://localhost:8000';

// No other exports needed - Axios instance, endpoints, error handler are managed within taskApi.ts